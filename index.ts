// n8n discovers nodes/credentials via the "n8n" block in package.json
// (pointing directly at the compiled files), not through this entry
// point. It exists only so `main` in package.json resolves to something.
export {};
